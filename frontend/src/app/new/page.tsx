// Backward-compatible alias for the New Design page (canonical: /designs/new).
// Renders the shared component so existing /new?prompt=... deep links keep working.
export { default } from "@/components/NewDesign";
